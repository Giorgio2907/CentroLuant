@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="d-flex flex-column align-items-center justify-content-center text-center py-5">
    <div class="mb-3" style="font-size: 4rem; color: #dc3545;">
        🚫
    </div>
    <h2 class="mb-2" style="color: #dc3545;">Acceso denegado</h2>
    <p class="text-muted mb-4" style="max-width: 420px;">
        Tu usuario no tiene permisos suficientes para ver esta sección.
        Si crees que esto es un error, comunícate con el administrador del sistema.
    </p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary me-2">Volver al inicio</a>
    <a asp-controller="Account" asp-action="Logout" class="btn btn-outline-danger">Cerrar sesión</a>
</div>
